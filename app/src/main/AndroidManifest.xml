<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.folyfinalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JoinInfogoogleActivity"></activity>
        <activity android:name=".MapActivity">
            <meta-data
                android:name="com.naver.maps.map.CLIENT_ID"
                android:value="nnv85idnj2" />
            <meta-data
                android:name="com.naver.maps.map.CLIENT_TYPE"
                android:value="gov" />
        </activity>
        <activity android:name=".JoinActivity" />
        <activity android:name=".JoinInfoActivity" />
        <activity android:name=".IdFindActivity" />
        <activity android:name=".PasswordFindActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>